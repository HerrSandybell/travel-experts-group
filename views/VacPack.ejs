<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/meta-data'); %>
    <%- include('./partials/style-sheets'); %>
    <link rel="stylesheet" href="./css/vacation-packages.css">
    <title>Vacation-Packages | Travel Experts</title>
    <style>
    </style>
  </head>

<body>
  <div class='main-content'>

    <%- include('./partials/navigation'); %>

    <header class="hero-section center flex-column background">
      <h1>TRAVEL EXPERTS</h1>
      <p>Thinking of a last-minute vacation?</p>
    
      <p>Our travel deals can take you there for less!</p> 
      <a class="button" href="registration">SIGN UP NOW!
        <span></span><span></span><span></span><span></span>
      </a>
    </header>

    <main class="center">
      <div class="vac-packages">
        <% for (let package of packages) { %>
        <figure class="package-container" id="<%= package.Id %>"> 
          <div style="background-image: url('http://localhost:3000/images/<%= package.imageFile %>')"><img src="" width="300" height="300"></div>
          <figcaption>
            <h2><%= package.packageName %></h2>
            <p class="description"><%= package.packagedDescription %></p> 
            <p class="duration-price"><%= package.duration %> nights for <span class="price">$<%= package.packagePrice %>.</span></p>
            <p><span>Book By:</span> <%= String(package.packageStartDate).slice(4, 15) %></p>
            <p><span>Valid Until:</span> <%= String(package.packageEndDate).slice(4, 15) %></p>
            <button class="button">BOOK NOW!</button>
          </figcaption>
        </figure>
        <% } %>
      </div>
    </main>
 
    <%- include('./partials/footer'); %>

  </div>
  <%- include('./partials/scripts'); %>
</body>
</html>